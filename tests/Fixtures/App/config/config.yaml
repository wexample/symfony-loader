framework:
  test: true
  secret: "test"

wexample_symfony_loader:
  front_paths:
    - tests/Fixtures/front/

twig:
  default_path: '%kernel.project_dir%/templates'

services:
  Wexample\SymfonyHelpers\Service\Entity\EntityNeutralService:
    autowire: false
    arguments:
      - '@?doctrine.orm.entity_manager'
