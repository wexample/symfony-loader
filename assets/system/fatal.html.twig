<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Assets missing</title>
        <style>
            html, body {
                height: 100%;
                margin: 0;
            }
            body {
                display: flex;
                align-items: center;
                justify-content: center;
                background: #000;
                color: #FFF;
                font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
                    "Liberation Mono", "Courier New", monospace;
            }
            .container {
                display: flex;
                flex-direction: column;
                gap: 8px;
                max-width: 640px;
                width: auto;
                padding: 24px;
                box-sizing: border-box;
                text-align: left;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div>[X] {{ message }}</div>
            {% if hint %}
                <div>{{ hint }}</div>
            {% endif %}
        </div>
    </body>
</html>
